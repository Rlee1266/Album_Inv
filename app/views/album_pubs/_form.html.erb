<%= form_with(model: album_pub, local: true) do |form| %>
  <% if album_pub.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(album_pub.errors.count, "error") %> prohibited this album_pub from being saved:</h2>

      <ul>
      <% album_pub.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
