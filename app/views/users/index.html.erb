<p id="notice"><%= notice %></p>

<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
  <% @user.each do |user| %>
    <li>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>

<br>

<%= link_to 'New User', new_user_path %>
<%= link_to 'Albums', album_invs %>
